{
  "$id": "https://mastory.io/schemas/mixins/ActivityControlled.json",
  "properties": {
    "controllingActivity": {
"description": "The ID of an activity fragment which is consulted to define this fragment's support, instructions, and submit behavior.",
      "$ref": "../core/identifier.json"
    }
  }
}