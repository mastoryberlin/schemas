Returns a JSON array of objects, each with a `name` and a `description` field,
that describe all available Vue components.
Their `name` can be directly used in Vue templates thanks to Nuxt auto-import feature.