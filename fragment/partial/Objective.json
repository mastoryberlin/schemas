{
  "properties": {
    "text": {
      "type": "string"
    },
    "role": {
      "type": "string",
      "enum": [
        "mandatory",
        "supportive",
        "optional"
      ]
    },
    "activityId": {
      "type": "string"
    },
    "showSupportives": {
      "oneOf": [
        {
          "type": "boolean"
        },
        {
          "type": "string",
          "enum": [
            "atOnce",
            "oneByOne"
          ]
        }
      ]
    }
  },
  "required": [
    "text"
  ]
}