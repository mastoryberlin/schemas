{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "move": {
"description": "Whether users can move this fragment. * For DOM and canvas fragments, this typically involves dragging them to a new screen or canvas position. For 3D fragments, defines if the user may grab the object and place it somewhere else.",
      "oneOf": [
        {
          "type": "boolean"
        },
        {
          "$ref": "./only-except.json"
        }
      ],
      "default": true
    }
  }
}